## 4.3.6

- Исправлена отправка сообщения пользователю из веб-панели с PostgreSQL: значение `message_log.success` теперь передаётся как native boolean, без ошибки `DatatypeMismatch`.
- Добавлена автоматическая нормализация старой колонки `message_log.success` из integer/text в boolean при запуске миграции схемы.
- Интерфейс личного чата теперь корректно показывает HTTP-ошибку, даже если сервер вернул HTML или plain text вместо JSON.
- Добавлены regression-тесты для PostgreSQL boolean-контракта, миграции схемы и безопасного разбора ответа.

---

## 4.3.4

- Исправлена публикация обновлённой версии в GitHub `main`: PATCH-запрос теперь использует корректный Git refs endpoint.
- Добавлен regression test на корректный endpoint обновления ветки.

---

# Changelog

## 4.3.4 hotfix
- Исправлен установщик: новая установка больше не завершается ошибкой, если API-токен 3x-ui оставлен пустым для последующей настройки через веб-панель.
- Онлайн-проверка 3x-ui по-прежнему обязательна, когда токен уже настроен.

## 4.3.3

- Исправлена автоматическая синхронизация публичной ветки `main` после публикации обновления через веб-панель.
- `main` обновляется одним Git commit через Git database API GitHub без force-push.
- Перед переключением `main` повторно проверяется исходный commit, чтобы не перезаписать параллельные изменения.
- При ошибке синхронизации панель не сообщает об успешной полной публикации обновления.

## 4.3.1

- Добавлен bootstrap-установщик для установки актуального релиза через `wget`.
- Исправлены подтверждённые проблемы установщика, TLS-default и состояния Yandex backup.
- Обновлены README и release-документация.
- Удалены исторические audit/release-note документы из production-архива.
- Добавлена автоматическая публикация GitHub Release при push тега `FargoVPN-*`.
