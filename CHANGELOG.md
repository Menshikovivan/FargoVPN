## 4.4.5

- Исправлена регистрация нового пользователя по 4-значному коду: подтверждение и меню отправляются сразу после фиксации пользователя в БД.
- Создание реферальной записи и журналирование вынесены из критического пути ответа; их ошибка больше не оставляет пользователя без сообщения после успешной регистрации.
- При реальной ошибке записи регистрации бот теперь сообщает о временной ошибке и предлагает повторить ввод кода вместо молчания.

---

## 4.4.4

- Убрана задержка Telegram-обработчиков из-за синхронной записи журнала; добавлены предупреждения для обработчиков и PostgreSQL-запросов дольше настраиваемого порога.
- После успешной регистрации по коду бот подтверждает авторизацию и показывает владельца кода по цепочке username, имя, Telegram ID; добавлено обратно совместимое поле `users.display_name`.
- Для вкладки «Оплаты» добавлен обновляемый счётчик платежей со статусом `pending`/`processing`, а таблица оплат адаптирована в карточки для экранов 320–480 px.
- Бэкапы Яндекс.Диска переведены на запись только в заранее смонтированную через `davfs2` папку с ограничением времени; приложение больше не изменяет secrets/fstab и не выполняет mount/umount.
- Все новые клиенты 3x-ui создаются с пустым `flow`; существующие клиенты при обновлении сохраняют своё текущее значение.

---

## 4.4.3

- Яндекс.Диск переведён на системный `davfs2` с `/etc/davfs2/secrets` и `/media/YandexDisk` согласно штатному способу подключения Yandex WebDAV.

## 4.4.3

- Исправлены дубли чатов во вкладке «Сообщения»: диалоги теперь уникальны по `tg_id`, а смена username не создаёт второй чат.
- Исправлена изоляция каналов доставки резервной копии: ошибка Яндекс.Диска не блокирует Telegram и наоборот.
- Исправлена обработка пустого `BACKUP_AGE_RECIPIENT`: backup worker корректно определяет recipient по локальному age-ключу.
- Добавлены regression-тесты для обоих исправлений.

---

## 4.4.2

- Исправлены кнопки «Проверить подключение» и «Тестовая загрузка» в настройках Яндекс.Диска: отдельные CSRF-защищённые JSON API и неблокирующий запуск WebDAV-проверок.
- Добавлен лёгкий live-раздел процесса бэкапа на странице журналов с созданием архива и доставкой в Telegram/Яндекс.Диск.
- Добавлен ограниченный атомарный live-state файл без постоянного обращения к `journalctl`.

---

## 4.4.1

- Исправлен полный backup-путь для legacy 3x-ui с SQLite: создаётся согласованный read-only snapshot `x-ui.db`, а не требуется несуществующий PostgreSQL DSN.
- WebDAV-загрузка переведена на прямой потоковый PUT с подтверждением фактического размера удалённого файла и явной диагностикой HTTP-кодов.
- Ошибки создания архива теперь попадают в `backup_runs`, а ручной и плановый запуск отправляют администратору итоговый статус.

---

## 4.4.0

- Исправлена ошибка `Недействительный CSRF-токен` при запуске массовой рассылки: собственный XHR-обработчик рассылки теперь передаёт session CSRF header.
- Проверена цепочка резервного копирования: Telegram-кнопка администратора использует `backup.py --force`, а systemd timer — тот же worker в режиме `--scheduled`; контракт службы дополнительно закреплён тестами.
- Приведены в соответствие с текущей реализацией WebDAV+PostgreSQL контрактные тесты, без изменения рабочей схемы данных.

---

## 4.3.6

- Исправлена отправка сообщения пользователю из веб-панели с PostgreSQL: значение `message_log.success` теперь передаётся как native boolean, без ошибки `DatatypeMismatch`.
- Добавлена автоматическая нормализация старой колонки `message_log.success` из integer/text в boolean при запуске миграции схемы.
- Интерфейс личного чата теперь корректно показывает HTTP-ошибку, даже если сервер вернул HTML или plain text вместо JSON.
- Добавлены regression-тесты для PostgreSQL boolean-контракта, миграции схемы и безопасного разбора ответа.

---

## 4.3.4

- Исправлена публикация обновлённой версии в GitHub `main`: PATCH-запрос теперь использует корректный Git refs endpoint.
- Добавлен regression test на корректный endpoint обновления ветки.

---

# Changelog

## 4.3.4 hotfix
- Исправлен установщик: новая установка больше не завершается ошибкой, если API-токен 3x-ui оставлен пустым для последующей настройки через веб-панель.
- Онлайн-проверка 3x-ui по-прежнему обязательна, когда токен уже настроен.

## 4.3.3

- Исправлена автоматическая синхронизация публичной ветки `main` после публикации обновления через веб-панель.
- `main` обновляется одним Git commit через Git database API GitHub без force-push.
- Перед переключением `main` повторно проверяется исходный commit, чтобы не перезаписать параллельные изменения.
- При ошибке синхронизации панель не сообщает об успешной полной публикации обновления.

## 4.3.1

- Добавлен bootstrap-установщик для установки актуального релиза через `wget`.
- Исправлены подтверждённые проблемы установщика, TLS-default и состояния Yandex backup.
- Обновлены README и release-документация.
- Удалены исторические audit/release-note документы из production-архива.
- Добавлена автоматическая публикация GitHub Release при push тега `FargoVPN-*`.
